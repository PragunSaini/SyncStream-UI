import React from 'react';

const testCompo = prop => {
  return (
    <div>
      <h1>This is my first component {prop.name}</h1>
    </div>
  );
  //   return <h1>This is my first component.ff {props.name}</h1>;
};

export default testCompo;
